env:
variables:
phases:
	install:
		commands:
	pre_build:
		commands:
			- npm install
	build:
		commands:
			- npm run build
	post_build:
		commands:
			- aws s3 sync build/ s3://tommibucket
			- echo "Build success!"
artifacts:
	files:
		- build/*